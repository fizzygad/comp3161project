{
  "build": {
    "builder": "nixpacks",
    "config": {
        "language": "python",
        "installCommand": "pip install -r requirements.txt",
        "startCommand": "python -m gunicorn --bind 0.0.0.0:$PORT DBMSProjectAPI:app"
    }
  },
  "deploy": {
    "startTimeout": 300
  }
}