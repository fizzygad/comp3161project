{
  "build": {
    "builder": "nixpacks",
    "config": {
      "installCommand": "pip install -r requirements.txt",
      "startCommand": "gunicorn --bind 0.0.0.0:$PORT DBMSProjectAPI:app"
    }
  },
  "deploy": {
    "startTimeout": 300
  }
}